const express = require('express');
const router = express.Router();
// router.get('/centers',userController.account);
// router.post('/center',userController.accountUpdate);
// router.put('/centerdata',userController.accountUpdate);
module.exports = router;
